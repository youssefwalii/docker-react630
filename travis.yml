sudo: required 
language: generic

services: 
   - docker

   before_install:
     - docker build -t youssefwalii/docker-react -f Dockerfile.dev .
  script:
    - docker run -e CI=true youssefwalii/docker-react npm run test